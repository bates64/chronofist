using UnityEngine;

public class CameraFollowLevel : MonoBehaviour {
    void Update() {
        // TODO: figure out which Level the player is in and switch to its vcam (create vcam if not exist)
    }
}
